#[allow(unused_imports)]
use proconio::{input, marker};

fn main() {
    input! {
        t: usize,
    }
    for _ in 0..t {
        input! {
            n: usize,
            k: usize,
            a: [u64; n],
        }
        // let mut seg = 
    }
}
